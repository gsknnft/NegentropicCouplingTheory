{
    "name": "negatropic-coupling-theory",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "console/**/*",
        "packages/*/**"
    ],
    "scripts": {
        "build": "pnpm -r run build",
        "build:console": "pnpm --filter negentropic-console build",
        "dev": "pnpm --filter negentropic-console dev:console",
        "lint": "pnpm -r run lint",
        "lint:fix": "pnpm -r run lint:fix",
        "typecheck": "pnpm -r run type-check",
        "test": "pnpm -r run test"
    },
    "devDependencies": {
        "@tensorflow/tfjs": "4.22.0",
        "rimraf": "^6.1.0",
        "chalk": "5.6.2",
        "telemetry-core": "link:./packages/telemetry-core",
        "@gsknnft/bigint-buffer": "link:../bigint-buffer",
        "@sigilnet/qwave": "link:../SigilNet/packages/QWave",
        "@types/node": "24.10.1",
        "typescript": "^5.2.2",
        "zod": "4.3.6",
        "vitest": "4.0.18"
    },
    "pnpm": {
        "overrides": {
            "glob": "^8.1.0"
        }
    },
    "engines": {
        "node": ">=20.0.0",
        "pnpm": ">=10.0.0"
    }
}
